<%= link_to link_text_for_display, href, **link_attr %>
<%= stanford_only_icon if stanford_only? %>
<%= casalini_text if casalini_text %>
<%= tag.div additional_text, class: 'additional-link-text' if additional_text %>
