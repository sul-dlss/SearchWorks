<%= header %>

<%= tag.div class: (%w[panel panel-default access-panel] + @classes).join(' '), **@html_attrs do %>
  <div class="access-panel-heading panel-heading">
    <h3>
      <%= title %>
    </h3>
  </div>
  <div class="panel-body">
    <%= body %>
  </div>
  <%= after %>
<% end %>
