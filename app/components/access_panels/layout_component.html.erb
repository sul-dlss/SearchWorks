<%= header %>

<%= tag.div class: (%w[card access-panel] + @classes).join(' '), **@html_attrs do %>
  <div class="access-panel-heading card-header">
    <h3>
      <%= title %>
    </h3>
  </div>
  <div class="card-body">
    <%= body %>
  </div>
  <%= after %>
<% end %>
