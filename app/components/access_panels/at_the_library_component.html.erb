<h2>At the library</h2>
<div data-controller="live-lookup" data-live-lookup-id="<%= document.live_lookup_id %>" data-live-lookup-url-value="<%= availability_index_path %>">
  <%= render AccessPanels::LibraryComponent.with_collection(libraries, document: document) %>
</div>
