<%= tag.dl(**@html_attrs) do %>
  <% courses.each do |course| %>
    <dt>Course</dt>
    <dd>
      <%= link_to_course_reserve_course(course) %>
    </dd>
    <dt>Instructor(s)</dt>
    <dd>
      <%= safe_join course.instructor, ', ' %>
    </dd>
  <% end %>
<% end %>
