<% if @documents&.one? %>
  <%= render 'catalog/single_citation_with_modal', { presenter: document_presenter(@documents.first) } %>
<% elsif @documents&.any? %>
  <%= render 'catalog/grouped_citations' %>
<% end %>
