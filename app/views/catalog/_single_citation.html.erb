<h3 class="modal-title"><%= document_heading(document) %></h3>
<% document.citations.each do |format, citation| %>
  <% unless format == 'NULL' %>
    <h4><%= format %></h4>
  <% end %>
  <%= citation %>
<% end %>
