<%= render Blacklight::System::ModalComponent.new do |component| %>
  <% component.title { document_presenter(document).heading } %>

  <% document.citations.each do |format, citation| %>
    <% unless format == 'NULL' %>
      <h4><%= format %></h4>
    <% end %>
    <%= citation %>
  <% end %>
<% end %>