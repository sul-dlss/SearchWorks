<% document ||= @document %>

<% if document.image_urls.present? %>
  <div class="document-thumbnail">
    <img class="stacks-image" src="<%= document.image_urls(:large).first %>" alt="">
  </div>
<% end %>
<%= render partial: "catalog/index_mods", locals: { document: document } %>
