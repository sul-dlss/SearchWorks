<ul class="facet-values list-unstyled blacklight-advanced-facet-select">
  <% display_facet.items.each do |item| -%>
    <li class="form-group">
      <div class="col-sm-9">
        <div class="checkbox">
          <%= label_tag "f_inclusive_#{facet_field.key}_#{item.value.parameterize}"  do %>
          <%= check_box_tag "f_inclusive[#{facet_field.key}][]", item.value.to_sym, facet_value_checked?(facet_field.key, item.value), :id =>  "f_inclusive_#{facet_field.key}_#{item.value.parameterize}"%>
            <%= "#{item.value} #{number_with_delimiter(item.hits)}" %>
          <% end %>
        </div>
      </div>
    </li>
  <% end %>
</ul>
